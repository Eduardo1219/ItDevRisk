# ItDevRisk

This application is using SQLite database, and the database is already in the source code. To run the application, simply debug in Visual Studio.

The application was developed using .NET CORE 8, EF and SQLite.

Okey Dokey, first comment
